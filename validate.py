import logging

class Validation:
    def __init__(self, userList):
        self.users = userList
    
    def netID(netID):
        if any(netID in acct for acct in self.users):
            return True
        else:
            return False

